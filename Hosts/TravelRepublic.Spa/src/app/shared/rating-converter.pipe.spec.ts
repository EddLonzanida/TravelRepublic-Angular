import { RatingConverterPipe } from './rating-converter.pipe';

describe('RatingConverterPipe', () => {
  it('create an instance', () => {
    const pipe = new RatingConverterPipe();
    expect(pipe).toBeTruthy();
  });
});
